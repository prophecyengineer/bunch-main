import { Card, Grid, Group, UnstyledButton } from "@mantine/core";
import { Activity } from "react-activity-feed/dist/components/Activity";
import styles from "/Activity.module.css";
const ActivityCard = ({ props }) => {};

export default ActivityCard;
